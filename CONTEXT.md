# HealthAgent 心理健康助手

面向用户的心理健康支持系统：用户与 AI 助手进行心理咨询对话，系统提供情绪分析与心理健康知识内容，并供管理端审查咨询记录。

## Language

### 咨询域

**咨询会话 (Consultation Session)**:
一名用户与 AI 助手之间的一次持久化对话，归属且仅归属一名用户。用户端只能看到自己的会话。
_Avoid_: 聊天会话、对话、conversation

**咨询消息 (Consultation Message)**:
咨询会话中的一条发言，发送者为用户或 AI 助手之一。
_Avoid_: 聊天记录、chat log

**临时会话 (Temporary Session)**:
仅存在于前端界面的对话占位，用户发出第一条消息后才落库成为咨询会话。
_Avoid_: 草稿会话

**活跃回复 (Active Reply)**:
一条正在生成、尚未完成的 AI 回复。约束：同一用户同一时刻至多一条；活跃回复存在期间不能发送新消息（在任何会话），切换、新建或删除会话不受限。断开连接不终止生成；用户可显式停止。完成或停止后，回复以已生成内容进入咨询消息记录。
_Avoid_: 与"咨询消息"混淆（进行中的回复尚未落库）；"正在输入"只是它的界面表现

**停止生成 (Stop)**:
用户主动终止活跃回复的动作。已生成部分成为该次回复的最终内容，进入咨询消息记录，与自然完成无异。只能由显式停止动作触发，与断开连接无关。
_Avoid_: 中断、取消（易与断开连接混淆——断开不终止生成）

**重连观看 (Re-attach)**:
页面刷新或切换会话后，重新订阅活跃回复的观察流、从已生成部分继续实时显示的能力。观察流可被多次订阅，订阅变化不影响生成。
_Avoid_: 断线重连（暗示恢复旧连接——实为新连接新订阅）

**会话时长 (Session Duration)**:
由会话内消息时间推导出的派生量，不是会话自身的固有属性。
_Avoid_: —

**软删除 (Soft Delete)**:
见"通用"词条。

**对话记忆 (Conversation Memory)**:
AI 助手参与一次咨询会话时携带的上下文。来源于咨询消息，是易失的，可由消息记录重建。
_Avoid_: 与"咨询消息"混用

**情绪分析 (Emotion Analysis)**:
对一个咨询会话近期消息情绪状态的结构化评估，以极性（负面/非负面）为纲：产出情绪短语、评分、风险等级与基调相应的建议（负面疏导安抚、正面鼓励保持）。
_Avoid_: 情绪报告、情绪标签（与消息上的 emotion_tag 混淆）

**情绪快照 (Emotion Snapshot)**:
一次情绪分析在某一时刻的完整结构化结果。生成后缓存于会话上供读取复用，直至会话产生新消息后才可能重新分析。
_Avoid_: 与"对话记忆"混用

### 情绪日记域

**情绪日记 (Emotion Diary)**:
用户对某一天自身情绪与生活状态的主动记录：自评情绪分、主要情绪、触发因素、感想、睡眠与压力指标。归属且仅归属一名用户；同一日期可写多条，允许补写过去日期，不允许未来日期。
_Avoid_: 与"咨询会话"混淆（日记不关联会话）

**自评情绪分 (Mood Score)**:
用户对当日整体情绪的主观评分，1-10。与情绪分析产出的情绪强度（0-100）是两套语义不同的度量，不得换算或混用。
_Avoid_: 情绪评分（与 AI 情绪强度混淆时）

**主要情绪 (Dominant Emotion)**:
用户从固定选项中选定的当日主导情绪，情绪日记的必填核心字段之一（另一为自评情绪分）。
_Avoid_: 情绪标签（与消息上的 emotion_tag 混淆）

**日记情绪分析 (Diary Emotion Analysis)**:
对一篇情绪日记的一次性 AI 结构化评估，结果与情绪快照同构并随日记落库。日记内容提交后不变，已有分析结果永不重算——这是与会话情绪分析（懒计算+缓存）的本质区别。分析缺失（提交时失败）时允许用户手动重试：重试是失败恢复，不改变一次性语义。
_Avoid_: 与会话的"情绪分析"混用

### 知识内容域

**知识文章 (Knowledge Article)**:
管理员创作的心理健康科普内容，面向全部访客公开（含未登录），不归属任何用户。生命周期为 草稿→已发布→已下线；已发布文章的编辑直接生效，不退回草稿。
_Avoid_: 与"情绪日记"混淆（日记是用户的私人记录，文章是公开内容）

**文章分类 (Category)**:
知识文章的归类维度，当前为单级；每篇文章归属且仅归属一个分类。
_Avoid_: 栏目、话题

**发布 (Publish)**:
管理员使文章对用户端可见的动作。首次发布记录首发时间，此后重新发布不覆盖。
_Avoid_: 上架

**下线 (Offline)**:
管理员收回文章可见性的动作；数据保留，可重新发布。文章仍是生命周期内的活跃对象（可编辑、首发时间保留）——与软删除不同，后者使文章整体退出流程、仅作留存记录。
_Avoid_: 下架、删除（与软删除混淆）

**首发时间 (First Published At)**:
文章第一次发布的时刻，一经写入不变。
_Avoid_: 发布时间（易与最后编辑时间混淆）

**阅读量 (Read Count)**:
知识文章在用户端被查看的累计次数。管理端预览不计入；不按访客去重。
_Avoid_: 观看人数（前端历史文案，实为次数而非人数）

**作者 (Author)**:
创作知识文章的管理员，展示名随其资料更新（优先昵称）。
_Avoid_: —

**封面图 (Cover Image)**:
知识文章的题图，可选。
_Avoid_: 头图

### 数据分析域

**看板 (Dashboard)**:
管理端的数据总览页，聚合全量用户数据（不按归属过滤），与用户端无对应界面。
_Avoid_: 数据大屏

**平均情绪分 (Average Mood Score)**:
看板与情绪趋势图的聚合指标，聚合源是用户的自评情绪分（1-10），不是 AI 情绪分析产出的情绪强度（0-100）。按日聚合时为当日全部日记自评情绪分的算术平均。
_Avoid_: 情绪评分（与情绪分析的情绪强度混淆时）

**活跃用户 (Active User)**:
统计窗口内有咨询会话行为或情绪日记记录的去重用户。当前窗口为近 7 天。
_Avoid_: 在线用户（无实时在线语义）

### 通用

**软删除 (Soft Delete)**:
对记录的退役式移除：从用户端消失，记录保留，管理端仍可查看（带已删除标记）。删除者：咨询会话与情绪日记为归属用户，知识文章为管理员。已删除的记录退出业务流程（会话不接受新消息、文章不可编辑与再发布）。
_Avoid_: 归档、与知识文章的"下线"混淆（下线是生命周期内的可逆状态）

**用户端 / 管理端**:
同一系统的两类界面与接口面。用户端面向登录用户本人（严格按归属过滤数据），管理端面向管理员（可查看全部用户数据）。两者接口分离，不共用端点。
_Avoid_: 前台/后台混用于指代接口
