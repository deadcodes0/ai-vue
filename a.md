## 后端亮点

- **AI 流式对话与断线重连：**基于 **Spring WebFlux Sinks** 构建可回放热流，配合 **SSE** 推送，将模型生成与订阅解耦，支持刷新/切页后重连续看。
- **单会话生成互斥与停止传播：**通过活跃流注册表保证同一会话仅一条生成流，支持主动停止并多端同步结束，防止重复调用 LLM。
- **情绪分析懒计算与并发去重：**按会话消息变化触发 **LLM** 分析并缓存结果，并发请求合并为同一任务，失败降级不阻塞对话。
- **LLM 结构化输出：**基于 **Spring AI ChatClient** 直接映射情绪分析对象，使用独立客户端避免污染对话记忆。
- **对话记忆重建：**从消息表回放最近上下文重建 **ChatMemory**，实现无状态服务下多轮连续对话。
- **双端认证与权限隔离：**基于 **Spring Security + JWT** 按路径隔离管理端/用户端，会话级校验资源归属，防止越权与 ID 枚举。
- **内容安全与数据治理：**富文本经 **Jsoup** 白名单清洗，统一 **UUID + 软删除** 管理数据生命周期。

## 前端亮点

- **SSE 流式续显：**基于 **fetch-event-source** 接收流式回复，切页不断流，刷新后重连续看，异常时回退消息列表兜底。
- **危机干预可视化：**识别高风险情绪后展示警示卡片并弹出心理援助热线，使用模态框保证关键告警不被遮挡。
- **Markdown 流式渲染：**流式分片实时增量渲染 **Markdown**，配合本地消息登记实现切走再切回的续显体验。