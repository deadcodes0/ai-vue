## 前端亮点（Vue 3）

- **AI 流式对话渲染：**基于 **@microsoft/fetch-event-source** 以 **POST + 携带 JWT** 建立 SSE 连接（原生 EventSource 只支持 GET 且无法带自定义头），逐 token 追加渲染打字机效果；设置 **openWhenHidden: true** 禁用库默认的"切页断流重发"行为，杜绝**重复请求**与消息重复。
- **断线重连续看：**刷新/切页后通过 attach 端点**重连活跃流**，先回放已生成内容再无缝续接实时输出，404 时自动回退消息列表接口兜底，保证任何场景下界面状态与后端一致。
- **状态管理：**基于 **Pinia** 管理登录态与角色信息，配合 **axios 请求/响应拦截器** 统一注入 Token、集中处理业务码——登录过期自动清除凭证并跳转登录页，业务组件零侵入。
- **富文本编辑器：**集成 **wangEditor 5** 实现知识库文章的图文混排编辑，上传图片对接后端文件接口按文章 UUID 归档；展示侧配合 **v-html + 后端 Jsoup 白名单清洗**，双端防护 XSS。
- **Markdown 渲染：**自研 **MarkdownRenderer** 组件渲染 AI 回复（LLM 输出天然是 Markdown），代码高亮与排版对齐主流 AI 对话产品体验。
- **数据可视化：**基于 **ECharts** 实现管理端数据分析看板（情绪趋势、用户活跃等），直观呈现平台心理健康态势。
- **工程化：**基于 **Vite 7 + Vue 3.5 组合式 API** 构建，`import.meta.url` 管理静态资源，**Element Plus 按需图标**引入，保持构建产物精简。

几点说明：

1. **含金量排序**：前两条（fetch-event-source 的 POST/JWT/切页配置、重连续看）是前端真正的差异化亮点，因为它们踩过真实的坑（`openWhenHidden` 默认行为导致重复请求是你记忆库里记录过的实际教训）；后几条属于标配，凑数用。
2. 前端整体亮点密度低于后端，**建议简历上前端只保留 2~3 条**，其余篇幅留给后端；或者干脆压缩成一句"前端采用 Vue 3 + Element Plus + Pinia 技术栈，重点实现 SSE 流式渲染与断线重连"。
3. 注意第 1 条没有写错方向——"用 fetch-event-source 而非原生 EventSource"本身就是个可讲的面试点：原生 API 不支持 POST、不能自定义 header，JWT 认证的 SSE 场景下它是标准解法。


- **流式对话续显与重连**：基于 **@microsoft/fetch-event-source** 接收 **SSE** 流式回复，设置 `openWhenHidden: true` 禁用切页断流重发；刷新/跨标签页场景调用 **attach 接口**重连观看，404 扑空时自动**回退拉取消息列表**兜底。
- **危机干预可视化**：情绪分析判定 **riskLevel=3（危机）** 时展示静态红卡并弹出 **12356** 全国心理援助热线，使用 **ElMessageBox 模态框**（而非可堆叠通知）确保关键告警不被遮挡。
- **打字机式 Markdown 渲染**：流式分片实时追加并增量渲染 **Markdown**，配合本地 inflight 消息登记实现"切走再切回"的**续显**体验。

- **流式消息防串台：** AI 流式回复的片段**只写连接创建时闭包绑定的气泡对象**、不写当前视图，配合 **Map 按会话键控登记在途消息** 实现切换会话后续显/互不污染；`done` 收尾动作先校验目标会话身份，404 扑空/连接异常**回源轮询对齐服务器状态**；SSE 客户端设置 **`openWhenHidden: true`** 禁用切后台断流重发，杜绝片段重复与错位。

如果嫌长，短版：

- **多会话流式防串台：** SSE 片段**闭包绑定写入目标**、在途消息按会话键控续显，切换/刷新场景零错位；异常时**回源对齐服务器状态**，配合 `openWhenHidden` 禁后台重发，杜绝重复流与消息串写。