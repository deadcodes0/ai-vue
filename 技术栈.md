Also check application.yml for model config (DeepSeek). I have memory: DeepSeek API. Quick answer suffices; maybe check application.yml briefly. I can rely on memory constraints. Just present tech stack.
基于 [pom.xml](file:///d:/Coding/health-agent/HealthAgent/ai-spingboot/pom.xml) 和 [package.json](file:///d:/Coding/health-agent/HealthAgent/ai-vue/package.json) 汇总，这个项目的技术栈如下：

## 项目技术栈

**项目定位**：面向大学生的 AI 心理健康助手（AI 咨询对话 + 情绪分析 + 情绪日记 + 知识科普库 + 管理后台）

### 后端（ai-spingboot）

| 类别 | 技术 | 版本 | 用途 |
|---|---|---|---|
| **框架** | Spring Boot | 3.5.15 | 基础框架（Java 17） |
| **AI 框架** | Spring AI（OpenAI starter） | 1.0.0 | LLM 接入：流式对话、ChatMemory、BeanOutputConverter 结构化输出 |
| **大模型** | DeepSeek（OpenAI 兼容协议） | — | 心理疏导对话 + 情绪分析，模型/密钥全部走 application.yml 配置 |
| **响应式** | Project Reactor | 随 Boot | Sinks 热流、SSE 流式推送 |
| **ORM** | MyBatis-Plus（spring-boot3-starter） | 3.5.7 | 数据访问、分页插件 |
| **数据库** | MySQL | 8.x | 业务存储（mysql-connector-j） |
| **安全** | Spring Security + java-jwt | 4.4.0 | JWT 无状态认证、路径级 RBAC（管理端/用户端隔离） |
| **校验/工具** | hutool-all / validation / Lombok | — | JSON 处理、参数校验、样板代码精简 |
| **安全清洗** | Jsoup | 1.17.2 | 富文本白名单清洗防 XSS |

### 前端（ai-vue）

| 类别 | 技术 | 用途 |
|---|---|---|
| **框架** | Vue 3.5（组合式 API）+ Vite 7 | 单页应用构建 |
| **UI 库** | Element Plus 2.13 | 组件库（按需引入图标） |
| **状态管理** | Pinia 3 | 登录态、角色信息 |
| **路由** | Vue Router 4 | 双端（用户/管理）路由 |
| **HTTP** | axios（拦截器）+ @microsoft/fetch-event-source | 常规请求 + **POST 带 JWT 的 SSE 流式连接** |
| **富文本** | wangEditor 5 | 知识文章图文编辑 |
| **可视化** | ECharts 6 | 数据分析看板 |
| **样式** | Sass | 样式预处理 |

### 一句话简历版（可直接用）

```markdown
**技术栈：**Spring Boot 3.5 + Spring AI 1.0（DeepSeek）+ Reactor SSE 流式架构 + Spring Security JWT + MyBatis-Plus + MySQL；前端 Vue 3 组合式 API + Vite + Element Plus + Pinia + ECharts + wangEditor，fetch-event-source 实现 SSE 流式渲染。
```

两点提示：

1. **面试口径**：写"Spring AI + DeepSeek"比只写 DeepSeek 好——Spring AI 是框架层（你用到了 ChatMemory、Advisor、结构化输出等特性），DeepSeek 只是模型提供方，分层表述显得架构感更强。
2. 前端技术栈里 **@microsoft/fetch-event-source 值得单独点出**，它是你这个项目前端唯一非"标配"的库，天然引出"为什么要用它而不用原生 EventSource"的面试话题。